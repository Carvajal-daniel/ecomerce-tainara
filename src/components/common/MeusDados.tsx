// src/components/MeusDados.tsx

import React from 'react';

// 1. Definimos uma interface para as props do componente.
// Isto garante que o componente recebe os dados esperados.
interface MeusDadosProps {
  nome: string;
  email: string;
  dataNascimento: string;
  morada: string;
}

// 2. Usamos a interface para tipar as props.
const MeusDados = () => {
  return (
    <div >
  
    </div>
  );
};

export default MeusDados;